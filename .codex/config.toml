# .codex/config.toml — project-scoped Codex settings (trusted projects only)
approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "cached"

# Optional: stricter reviews if you use Codex CLI /review
# review_model = "gpt-5-pro"

[shell_environment_policy]
include_only = ["PATH", "HOME", "USERPROFILE", "TMP", "TEMP"]
