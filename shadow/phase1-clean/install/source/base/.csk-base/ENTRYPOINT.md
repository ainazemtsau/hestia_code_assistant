# Shadow Entrypoint

Use the installed workflow entrypoint first.

- Fresh install into a new project: `csk-init`
- Install into an existing project: `csk-adopt`
- After updating the installed workflow base: `csk-project-update`
