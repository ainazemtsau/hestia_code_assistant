# Execution

Canonical documents in this subtree:

- `EXECUTION_ENTRY.md`
- `SLICE_DISCIPLINE.md`
- `INCIDENT_RULES.md`
- `STATE_AND_EVIDENCE.md`

These documents define:

- when execution-facing work may begin after the critic gate
- how leaf execution proceeds in disciplined small slices
- when incidents must be logged and how they change execution posture
- how execution keeps `state.yaml`, `dashboard.yaml`, and `evidence.md` aligned with real work

Stage 4 is complete when this subtree gives a contributor the full pre-READY execution contract without reopening planning or critic semantics.
